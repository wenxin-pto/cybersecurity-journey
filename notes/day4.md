# Day 4 Notes

## What I worked on
- Created and updated the `01-network-traffic-analysis` project repository.
- Used NextWork.org as a guided SOC analyst learning project.
- Installed and configured Wireshark and Git on Ubuntu VM.
- Set up packet capture on the active network interface (`enp2s0`).
- Captured live network traffic and explored Wireshark display filters.
- Investigated DNS traffic using the `dns` filter.
- Used `nslookup google.com` to generate DNS packets.
- Identified DNS query and response packets using the Query ID (QID).
- Reviewed Protocol Hierarchy Statistics to understand captured traffic patterns.

## What I learned
- How SOC analysts inspect live packet captures.
- How DNS requests and responses are matched in Wireshark.
- How display filters help isolate suspicious or useful traffic quickly.
- Basic Git and GitHub workflow for documenting cybersecurity projects.
